Flyway migrations, applied in order on application startup (see `spring.flyway` in `application.yml`).

Naming convention: `V1__description.sql`, `V2__description.sql`, ... (Flyway's standard `V<version>__description.sql`).
